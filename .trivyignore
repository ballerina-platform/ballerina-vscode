# Trivy ignore file for known vulnerabilities with no available fix.
# Format: CVE-ID or vulnerability ID
#
# Only list findings here when the upstream author has released NO fix
# (trivy reports an empty "Fixed Version"). Anything with a fix version
# must instead be bumped via globalOverrides in
# common/config/rush/pnpm-config.json. Keep entries in sync with the
# submodule's submodules/wso2-vscode-extensions/.trivyignore.
#
# A fix that exists but is unusable for the affected version (e.g. the only
# fixed release is an incompatible major) counts as "no fix" for the purposes
# of this file, but say so explicitly in the comment and note what would let
# the entry be removed.
#
# This file must stay in the plain one-ID-per-line format. Trivy's richer
# YAML ignore format supports per-package scoping and expiry dates, but
# aquasecurity/trivy-action refuses to mix YAML and plain ignore files in one
# run, and the submodule's ignore file (also passed to the scanner) is plain.

# cors-anywhere@0.4.4 — direct devDependency (local CORS proxy used in dev
# only, never built or shipped). SSRF is inherent to the tool and no fix has
# been released by the author. https://github.com/wso2/vscode-extensions/issues/550
CVE-2020-36851

# elliptic@6.6.1 — transitive dep of the browserify crypto stack
# (create-ecdh / browserify-sign). No fix released by the author; 6.6.1 is
# the latest published version.
CVE-2025-14505

# brace-expansion@1.1.16 and @2.1.2 — DoS via unbounded expansion length.
# The fix exists only in 5.0.8 and has not been backported to the 1.x/2.x
# lines (1.1.16 and 2.1.2 are the latest published releases of each). 5.x is
# not a drop-in replacement: it removed the CommonJS default export that
# minimatch 3.x/5.x/9.x call, so forcing those lines to 5.0.8 breaks them at
# runtime. The 5.x line IS fixed via globalOverrides, so this entry only
# covers the 1.x/2.x transitives. Remove it once upstream backports, or once
# nothing depends on minimatch below v10.
CVE-2026-14257
